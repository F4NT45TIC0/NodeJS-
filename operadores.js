    //operador + operação de adição 
    //operador - operação de subtração
    //operador * operação multiplacação
    //operação / operação divisão
    //operação = atribuição de valor
    //operação == só compara valores
    //operação === comparação de valor e tipo de dados

    //operação ||: Operador “ou”, retorna true caso uma condição seja válida;
    //operação &&: Operador “e”, retorna true somente se todas as condições forem válidas;
    //operação != e !==: Operadores “não igual” e “estritamente não igual”, utilizados para comparação, da mesma forma que == e === retornam true ou false.